defaults.url=https://sentry.io/
defaults.org=bocm
defaults.project=react-native

# IMPORTANT:
# Do NOT commit auth tokens to the repository.
# Provide credentials via CI/EAS secrets instead (e.g. SENTRY_AUTH_TOKEN),
# or disable/allow-failure for uploads in CI using:
# - SENTRY_DISABLE_AUTO_UPLOAD=true
# - SENTRY_ALLOW_FAILURE=true

